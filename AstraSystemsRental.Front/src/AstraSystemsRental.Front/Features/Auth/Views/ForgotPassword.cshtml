@{
    ViewData["Title"] = "Restablecer contraseña";
    Layout = "_LayoutAuth";
}

<h1 class="text-2xl mb-1">Restablece tu contraseña</h1>
<p class="text-sm text-content-soft mb-6">Te enviaremos por correo un enlace seguro para crear una nueva contraseña.</p>

<div id="forgot-container">
    @await Html.PartialAsync("_ForgotPasswordForm", new ForgotPasswordForm(string.Empty))
</div>
